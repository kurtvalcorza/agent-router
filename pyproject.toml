[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "agent-router"
version = "0.1.0"
description = "Policy-driven execution routing for cost-efficient AI agents"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
agent-router = "agent_router.cli:main"
agent-router-benchmark = "agent_router.benchmark_cli:main"

[project.optional-dependencies]
openai = ["openai>=2.0"]
anthropic = ["anthropic>=0.120"]
catalog = ["PyYAML>=6.0"]
providers = [
  "openai>=2.0",
  "anthropic>=0.120",
]
all = [
  "openai>=2.0",
  "anthropic>=0.120",
  "PyYAML>=6.0",
]
dev = [
  "build>=1.2",
  "pytest>=8.0",
  "ruff>=0.6",
  # The test suite and examples/quickstart.py load YAML catalogs, so the dev
  # environment needs the catalog extra's parser for CI to cover those paths.
  "PyYAML>=6.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/agent_router"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
